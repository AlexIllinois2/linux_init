## linux初始化脚本
- ### 安装项
- `zsh`和常用设置（含ssh和git）
- `fcitx5`+薄荷拼音
- ### 准备
- 需要换源的先自行搜索换源
- 如有`ssh`秘钥，提前将秘钥文件复制到`~/.ssh`下

  按需运行脚本

- 安装完成, 新打开一个终端会话以交互式设置`p10k`样式（执行`p10k configure`可重新设置样式）
- ### 注意事项
- 如果要卸载`zsh`, 卸载前需要先恢复默认`shell`:
    
  ```bash
  chsh -s "$(which bash)"
  ```
